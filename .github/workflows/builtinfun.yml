name: builtinfun
on:
  push:
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: task1
      if: contains(${{vars.ENV}},'dev')
      run: |
        echo "hello"
        echo ${{vars.ENV}}
