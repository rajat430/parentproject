name: errorHandling
on: 
  push:
  workflow_dispatch:
jobs:
  job1: 
    runs-on: ubuntu-latest
    steps:
     - name: successtask
       run: echo "success"
     - name: failuretask
       id: step2
       continue-on-error: true
       run: echo "failed
     - name: taskafterfailure
       if: ${{ failure() && steps.step2.conclusion == 'failure' }}
       run: echo "task after failure"
  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - name: task1
        run: echo "hello"
      - uses: rajat430/myaction@main
        




       
